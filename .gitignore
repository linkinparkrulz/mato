node_modules/
.DS_Store
*.log
dojobay_deploy
dojobay_deploy.pub

# live backend state (submissions, sessions, apikeys) — never commit
server/data/

# Generated by scripts/pack-source.mjs when the suites run: a zip of the source
# tree, which is instance output rather than source, and stale the moment the
# next commit lands.
data/*-src.zip
